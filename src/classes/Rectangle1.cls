public class Rectangle1 {
    Integer length;
    Integer width;
        public void setValues (Integer len, Integer wth){
             length = len;
             width = wth;
        }
        public void getValues() {
            system.debug('length =' + length);
            system.debug('width =' + width);
                         }
}