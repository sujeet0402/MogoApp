public class Square {
    // Suare has 4 sides with same length
    private Integer length;
    //private Integer width;
        public void setValues (Integer len){
               length = len;
        }
        public void getValues() {
            system.debug('length =' + length);  
        }
}